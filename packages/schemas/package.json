{
  "name": "@argus/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "ARGUS \u2014 kanonische Protobuf- und JSON-Schemas. Einzige Wahrheitsquelle f\u00fcr alle Datenstrukturen.",
  "license": "UNLICENSED",
  "scripts": {
    "generate": "buf generate --template buf.gen.ts.yaml",
    "lint": "buf lint && buf format --diff --exit-code",
    "typecheck": "tsc -p tsconfig.json",
    "build": "buf generate --template buf.gen.ts.yaml",
    "breaking": "./tools/breaking.sh"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "ts-proto": "^2.7.7",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0"
  }
}
