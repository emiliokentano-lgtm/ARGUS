[project]
name = "argus-geo"
version = "0.1.0"
description = "ARGUS — geteilte Geo-Helfer: H3-Kodierung, Huellrechtecke, Projektionen."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["argus_geo"]
